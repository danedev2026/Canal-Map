# Python
.venv/
__pycache__/
*.pyc

# Flutter / Dart
app/.dart_tool/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/local.properties
app/.idea/
*.iml

# Large build artifacts (regenerated by the pipeline; keep out of git)
app/build/app/outputs/

# OS
.DS_Store
Thumbs.db

# Keep: data/*.geojson, data/stoppages.json, basemap.pmtiles are small enough
# and are the shippable pipeline outputs — they are NOT ignored on purpose.
